/**
 * safeRenderer.ts — RE-EXPORT ONLY
 *
 * Canonical implementations live in ./utils.ts (SSOT).
 * This file exists only for backwards compatibility.
 */
export { normalize, renderSafe } from './utils';
